name: CI

on:
  push:
    branches: [ develop, release/**, main, master, feature/**, issue/**, issues/**, dependabot/** ]

env:
  NODE_VERSION: "18.x"
  PYTHON_VERSION: "3.11"
  JUPYTERLAB_VERSION: "~=4.2.5"

jobs:
  call-build:
    uses: MAAP-Project/algorithms-jupyter-extension/.github/workflows/build.yml@develop
    secrets: inherit


#    jlpm install
#    pip install -e . -v
#    jupyter labextension develop . --overwrite
#    jlpm playwright install
#    jlpm run test --passWithNoTests